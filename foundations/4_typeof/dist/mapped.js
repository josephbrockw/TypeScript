let mathOperations = {
    add(x, y) {
        return x + y;
    },
    subtract(x, y) {
        return x - y;
    },
    multiply(x, y) {
        return x * y;
    },
    divide(x, y) {
        return x / y;
    }
};
let mathResults = {
    add: mathOperations.add(2, 3), // 5
    subtract: mathOperations.subtract(5, 3) // 2
};
export {};
//# sourceMappingURL=mapped.js.map